import { isValid, validForm, validNumFrames } from "../app/utilities/validators";
import { endIsLaterThanStart, isValidInput, validateAllInputs } from "../app/utilities/validators";

describe("Validator", () => {
	expect(2).toBe(2);
});